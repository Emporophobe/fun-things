/*
package FunThingGeneratorModel;

import java.util.List;

*/
/**
 * Interface for generating suggestions for activities.
 *//*

public interface IGenerator {

    */
/**
     * Pick a random fun thing from one of the given categories that fits the given parameters.
     *
     * @param participants The number of participants.
     * @param maxMinutes   The maximum length of time to do the activity, in minutes.
     * @param maxCost      The maximum cost of the activity, in dollars.
     * @param isOutside    Whether the activity requires putting on shoes.
     * @param categories   The categories we are ok with doing.
     * @return An IFunThing that matches all the parameters.
     *//*

    static IFunThing generate(int participants,
                       int maxMinutes,
                       int maxCost,
                       boolean isOutside,
                       List<Category> categories);
}
*/
